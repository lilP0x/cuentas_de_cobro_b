# Environment variables for local development
# MongoDB running in Docker, port forwarded to host (command used:
# docker run -d --name mongodb -p 27017:27017 mongo:6.0)
# Use localhost when connecting from the host machine.
MONGO_URI=mongodb://localhost:27017
# Optional: choose the database name (defaults to 'facturacion' in db.py)
MONGO_DB=facturacion

# Other optional settings
# DEBUG=true

# Comma-separated list of allowed origins for CORS. Use * to allow all.
# Examples:
# ALLOWED_ORIGINS=http://localhost:3000,https://mi-dominio.com
# ALLOWED_ORIGINS=*
ALLOWED_ORIGINS=*